@model IEnumerable<ProjetBDMuretDave.Models.Emploi>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeEmploi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Entreprise.Denomination)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personne.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeEmploi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Entreprise.Denomination)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personne.Nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEmploi }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEmploi }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEmploi })
        </td>
    </tr>
}

</table>
